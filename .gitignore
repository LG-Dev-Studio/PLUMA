# Dependencias
/vendor/
/node_modules/

# Build de producción (se genera reproducible en CI; no se versiona)
/build/
/dist/
/panel/dist/
/*.zip

# PHP-Scoper
/build-scoped/

# Calidad y caché de herramientas
/.phpunit.cache/
/.phpunit.result.cache
/.phpcs-cache
/.php-cs-fixer.cache

# wp-env
/.wp-env-override/
.wp-env.override.json

# NCP · Reorientación (ADR 0024): dataset de entrenamiento descargado a mano
# (InferES, ~2.4 MB) — el ARTEFACTO entrenado sí se versiona (recursos/modelos/).
/tools/entrenamiento-nli/dataset/

# Playwright
/playwright-report/
/test-results/
/blob-report/

# Entorno local
.env
.env.local
*.log
levantar-servidor.bat

# Sistema operativo / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Estado de sesión de Claude Code (no es parte del proyecto)
.claude/scheduled_tasks.lock
