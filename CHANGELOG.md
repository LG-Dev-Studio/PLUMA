# CHANGELOG — PLUMA Engine
Formato: [Keep a Changelog] · Versionado: SemVer. Cada entrada se escribe para el cliente, no para el desarrollador.

## [Unreleased]
### Añadido
- Capa de gobernanza completa del proyecto (CLAUDE.md, GOVERNANCE.md, AGENTS.md, SKILLS-STACK.md, ECOSYSTEM.md, PLAN-MAESTRO.md, skills `pl-*` y `lg-*` en `.claude/skills/`).
- Libro de Arquitectura v1.0 como fuente de producto (`docs/`).
- **Etapa 0 — Cimientos**: esqueleto instalable del plugin.
  - Kernel: `Contenedor` (DI propio), `Activador`/`Desactivador`/`Desinstalador` (ciclo de vida completo, multisitio), `Capacidades` propias (`pluma_gestionar_periodistas`, `pluma_aprobar_piezas`, `pluma_configurar_motor`), `RelojInterface`/`RelojSistema`, `DetectorEntorno`.
  - `Pluma\Datos\Migrador`: infraestructura de `pluma_db_version` con `dbDelta` idempotente (sin tablas todavía — nacen en la Etapa 1).
  - `Pluma\Admin\PantallaSalud`: primera pantalla real del panel (Sala de Máquinas — Salud del sistema), React + Vite, capacidad `pluma_configurar_motor`, i18n completa.
  - Toolchain de calidad: PHPCS (WordPress-Extra + Security), PHPStan nivel 8 (+ stubs de WordPress), PHPUnit + Brain\Monkey (28 tests Unit), PHPUnit de integración vía `wp-env` (`CicloDeVidaTest`), Vitest (4 tests), PHP-Scoper.
  - Empaquetado reproducible (`bin/build-zip`) y CI en GitHub Actions (calidad PHP, calidad JS, integración wp-env, empaquetado + smoke de instalación/activación/desactivación/desinstalación + Playwright E2E).
  - `LICENSES-THIRD-PARTY.md` (sin dependencias de producción todavía).
  - **Cerrada el 2026-07-22**: CI en verde en GitHub Actions (run [29968501162](https://github.com/jhonnfrank1995/PLUMA/actions/runs/29968501162)) — los 4 jobs (calidad PHP, calidad JS, integración wp-env, empaquetado) en `success`. Criterio de salida del PLAN-MAESTRO cumplido: el ZIP construido reproduciblemente se instala, activa, desactiva y desinstala limpio sobre un WordPress real, conservando los datos del cliente por defecto tal como exige GOVERNANCE §5.4.
- **Etapa 1 — El esqueleto que camina**: una tendencia real recorre el pipeline de punta a punta hasta un borrador WordPress trazable.
  - `Pluma\Pipeline`: `EstadoPieza` (grafo completo, `references/estados.md`), `Pieza` (DTO inmutable), `Transicionador` (única puerta de transición, candado por-Pieza optimista, auditoría, eventos `pluma/pieza_*`), `Orquestador` (detecta, avanza el pipeline por lotes, presupuesto de tiempo con corte limpio, candado global).
  - `Pluma\Datos\CandadoGlobal`: candado del orquestador vía `GET_LOCK()`/`RELEASE_LOCK()` de MySQL — sobrevive a que el proceso PHP muera a mitad de lote sin depender de que un `finally` llegue a ejecutarse.
  - Esquema nuevo (`Pluma\Datos\Esquema`, versión `0.2.0`): tablas `pluma_tendencias`, `pluma_piezas`, `pluma_fuentes`, `pluma_bitacora_motor`, `pluma_auditoria`.
  - `Pluma\Proveedores\ProveedorGoogleTrends`: primer Sensor de Radar, sobre el feed público de tendencias diarias de Google (sin credenciales), con circuit breaker por fallos consecutivos y validación SSRF (`ValidadorUrl`).
  - `Pluma\Sensores\SensorGoogleTrends` + `PuntuacionOportunidad`: puntuación de Velocidad + Afinidad (Hueco competitivo y Vida útil quedan en `docs/deuda.md`, dependen del Motor SEO).
  - `Pluma\Investigacion\InvestigadorMecanico` + `Pluma\Redaccion\RedactorMecanico`: expediente y borrador construidos mecánicamente desde los hechos reales del Sensor — sin periodista sintético (llega en la Etapa 2) ni llamadas a un proveedor de lenguaje, cero contenido inventado.
  - `Pluma\Publicacion\CreadorBorrador`: único punto con `wp_insert_post`, siempre en modo Piloto (`post_status=draft`).
  - `Pluma\Admin\RestOrquestador`: endpoint `pluma/v1/motor/tick` (GET/POST) con token rotable (`X-Pluma-Token`), rate limit y presupuesto de tiempo configurable — el punto de entrada del cron real del servidor.
  - `docs/hooks.md`: primer registro de eventos públicos del plugin.
  - Tests: 26 nuevos Unit (Transicionador, Orquestador, Sensor/Proveedor con fixture real del feed, Puntuación) + Integración contra WordPress real (doble ejecución simultánea, muerte a mitad de lote, endpoint sin token/con token inválido/válido, repositorios `pluma_piezas`/`pluma_tendencias`).
  - **Cerrada el 2026-07-23**: CI en verde en GitHub Actions (run [29973288960](https://github.com/jhonnfrank1995/PLUMA/actions/runs/29973288960)) — los 4 jobs (calidad PHP, calidad JS, integración wp-env, empaquetado) en `success`. Criterio de salida del PLAN-MAESTRO cumplido: una tendencia real detectada por el Sensor de Google Trends termina como borrador WordPress trazable de punta a punta; doble ejecución simultánea y muerte a mitad de lote verificadas contra MySQL real.
- **Etapa 2 — El periodista**: banco de periodistas sintéticos con diales de conducta, Algoritmo de Decisión Editorial, redacción en dos pasadas con Corrector Interno, memoria editorial y Bloque del Editor.
  - `Pluma\Proveedores`: contrato del Proveedor de Lenguaje (`LenguajeInterface`) + `ProveedorOpenRouter` (acceso a cualquier modelo de cualquier IA vía OpenRouter), `PropositoLenguaje` (enrutamiento por coste: clasificar barato, redactar con el mejor), `NeutralizadorMaterial` (anti-inyección de prompts con centinela aleatorio + saneado), `PresupuestoLenguaje` (verificado ANTES de cada llamada, aviso al 80%), `EnrutadorModelos`.
  - `Pluma\Kernel\Cifrado`: llaves de API cifradas en reposo con libsodium, clave derivada de las salts de `wp-config.php` — nunca en texto plano en `wp_options`.
  - Esquema `0.3.0`: tablas `pluma_periodistas`, `pluma_periodistas_conducta_versiones` (Conducta versionada e inmutable), `pluma_memoria_editorial`, `pluma_borradores`, y `pluma_piezas` ampliada con `periodista_id`, `periodista_version_id`, `ficha_decision_editorial`.
  - `Pluma\Redaccion`: modelo completo del periodista (Identidad, Diales, Reglas de Conducta, Matriz de Tonos con bloqueo de sátira en Tragedia como regla de sistema inviolable), `CompiladorDirectrices`, `PlantillasSiembra` (banco inicial recomendado: analista de datos, columnista crítica, cronista satírico).
  - `DecisionEditorial`: los 4 pasos del Libro Cap. 5.5 — `ClasificadorNoticia`, `AsignadorPeriodista`, `SelectorAngulo` (con "memoria antes de tesis": las posturas previas se consultan antes de elegir ángulo), `GeneradorEsqueleto` — producen la Ficha de Decisión Editorial completa de cada Pieza.
  - `RedactorSintetico` + `CorrectorInterno` (lista de verificación de 6 puntos: hechos, proporción interpretativa, solapamiento n-grama, voz, titular honesto, matriz/líneas rojas) + `VerificadorVoz`/`VerificadorNGramas` (deterministas) + `GeneradorBloqueEditor`. Máximo 2 ciclos de revisión; al tercero, la Pieza se marca RETENIDA — nunca se publica "lo menos malo".
  - `AvisoTransparenciaIa` y cita/enlace mecánico de toda fuente usada, en cada pieza redactada (GOVERNANCE §2.5/§2.6).
  - `RedactorConFallbackMecanico`: si el proveedor de lenguaje no tiene presupuesto o credenciales, notifica (`pluma/redactor_fallback_mecanico`) y usa el redactor mecánico de la Etapa 1 — decisión explícita del propietario, nunca bloquea el pipeline.
  - `Pluma\Admin\RestBancoPeriodistas`: export/import completo del banco de periodistas y su memoria (`GET`/`POST /pluma/v1/periodistas/{exportar,importar}`), protegido con la capacidad `pluma_gestionar_periodistas`.
  - `tests/Invariantes`: suite dedicada a GOVERNANCE §2 (anti-alucinación, bloqueo de sátira, cita/enlace de fuentes, transparencia de autoría IA) — criterio de salida explícito de la Etapa 2 en PLAN-MAESTRO.
  - Deuda nueva registrada en `docs/deuda.md` (PLUMA-E2-1 a E2-4): mejora futura del redactor mecánico de fallback, afinidad semántica real en la asignación de periodistas, compromiso de respuesta del Bloque del Editor, pantalla de administración para la llave de OpenRouter.
  - **Cerrada el 2026-07-23**: CI en verde en GitHub Actions (run [29981885310](https://github.com/jhonnfrank1995/PLUMA/actions/runs/29981885310)) — los 4 jobs (calidad PHP con `test:invariantes`, calidad JS, integración wp-env, empaquetado) en `success`. Criterio de salida del PLAN-MAESTRO cumplido: banco de periodistas con decisión editorial y redacción de dos pasadas operativo, invariantes GOVERNANCE §2 en verde, export/import del banco funcionando contra WordPress real.
- **Etapa 3 — La capa competitiva**: el pipeline completo (RADAR → INVESTIGADOR → SALA DE REDACCIÓN → MOTOR SEO → TAXÓNOMO → COMPUERTAS → PUBLICADOR), con las tres Compuertas como sistema inmunológico, modos reales con degradación por sensibilidad, cadencia de publicación real y Sala de Revisión.
  - `Pluma\Compuertas`: `CompuertaCalidad` (reaprovecha el juicio del Corrector Interno + veto duro por falta de sustento — una pieza sin trazabilidad al expediente NUNCA aprueba sin importar su puntuación), `CompuertaRiesgo` (sensibilidad temática con degradación automática de modo, difamación y hechos disputados → RETENIDA para humano, temas legalmente regulados), `CompuertaOriginalidad` (solapamiento n-grama contra fuentes y contra el propio sitio, huella de ganancia de información), `GestorDegradacion` (Autónomo→Copiloto por sensibilidad, Piloto forzado ante menores — ninguna configuración de usuario lo anula), `EvaluadorCompuertas` como único camino legal hacia Publicación.
  - `Pluma\Seo`: `ExtractorPalabrasClave`, `GeneradorMetadatosSeo` (doble titular: editorial con voz del periodista + SEO orientado a búsqueda, en una sola llamada consolidada), `ConstructorEsquemaNewsArticle` (JSON-LD NewsArticle/OpinionNewsArticle/AnalysisNewsArticle según el tono, verificado contra schema.org y la guía de Google), `DetectorPluginSeo` (Yoast/Rank Math verificados contra su código fuente oficial — PLUMA escribe en sus campos reales, nunca duplica su capa), `EnlazadorInterno` (reutiliza la memoria de cobertura del periodista), `AuditorCanibalizacion`, `MotorSeo` como orquestador único.
  - `Pluma\Taxonomia`: `ExtractorEntidades` (heurística determinista de secuencias capitalizadas), `ReconciliadorVocabulario` (coincidencia exacta, alias, similitud — nunca fragmenta etiquetas), `GestorEtiquetas` (etiquetas nuevas nacen en cuarentena hasta 3+ piezas), `AsignadorCategoria` (jamás crea una categoría — solo asigna de las que el editor ya definió).
  - `Pluma\Pipeline\Orquestador` real: cadencia elástica (cuota objetivo/mínima/máxima), ventanas horarias con peso, separación mínima + jitter, topes por vertical y por periodista (`ProgramadorCadencia`, `LectorConfiguracionCadencia`, tabla `pluma_cola_publicacion`); modos Piloto/Copiloto/Autónomo reales con ventana de veto configurable en Copiloto; escasez honesta (nunca rebaja umbrales para rellenar cuota, registra el déficit).
  - `Pluma\Publicacion\Publicador`: convierte el borrador en post publicado real, aplicando la capa SEO (`EscritorCamposSeo`) y la taxonomía (`AsignadorTaxonomiaWp`, categorías/etiquetas nativas de WordPress) ya calculadas.
  - `Pluma\Pipeline\GestorSalaRevision` + `Pluma\Admin\RestSalaRevision`: bandeja de piezas RETENIDAS y cola de veto de Copiloto, con tres acciones (`aprobar`/`devolver con nota`/`descartar`), protegidas con la capacidad `pluma_aprobar_piezas`. `Pluma\Admin\NotificadorRevision`: aviso por correo (`wp_mail`) cuando una pieza queda retenida.
  - Esquema `0.7.0`: tablas `pluma_vocabulario` y `pluma_cola_publicacion`; `pluma_piezas` ampliada con `modo_efectivo`, `diagnostico_compuertas`, `keyword_principal`, `datos_seo`, `resultado_taxonomia`.
  - `tests/Invariantes`: cierran GOVERNANCE §2 completo — §2.1 (toda decisión de compuerta se persiste y audita), §2.2 segunda capa (bloqueo de sátira también en Compuertas), §2.3 (difamación → RETENIDA sin importar el modo), §2.7 (escasez honesta nunca toca los umbrales).
  - Deuda nueva registrada en `docs/deuda.md` (PLUMA-E3-1 a E3-8): sitemap de noticias, imagen destacada, encabezados como preguntas, render del JSON-LD en el frontend, bucle de retroalimentación con Search Console, modo pausa/modo respeto, reintentos con retroceso exponencial, detección de WP-Cron real.
  - **Cerrada el 2026-07-23**: CI en verde en GitHub Actions (run [30008246143](https://github.com/jhonnfrank1995/PLUMA/actions/runs/30008246143)) — los 4 jobs (calidad PHP, calidad JS, integración wp-env, empaquetado) en `success`. Criterio de salida del PLAN-MAESTRO cumplido: una semana en Copiloto sin corrección posterior queda habilitada por la Sala de Revisión + ventana de veto reales; suite de invariantes GOVERNANCE §2 completa y matriz de compatibilidad SEO (Yoast/Rank Math) verificadas contra WordPress real.
- **Etapa 4 — La experiencia premium**: el panel completo del Cap. 10, entregado por porciones verticales (cada pantalla llega 100% funcional), más el onboarding de 5 actos del Cap. 10.3.
  - **La Portada** (Cap. 10.2, "el día de un vistazo"): shell del panel React con barra de estado persistente (modo activo, cuota del día, próxima publicación, coste contra presupuesto — Cap. 10.1: "el editor debe saber en tres segundos si todo está bien"), navegación por hash entre Portada y Sala de Máquinas, kanban compacto de piezas por estado, alertas de retenidas/fallidas con sus motivos, tendencias calientes, y aviso de déficit de cuota. Estilos editoriales propios (serif para titulares, sans para datos, color con significado, modo oscuro nativo, responsivo móvil) sin ninguna dependencia externa.
  - `Pluma\Admin\RestPortada` (`GET /pluma/v1/panel/portada`, capacidad `pluma_configurar_motor`): agrega modo de operación, cuota elástica de hoy contra la cola real, salud del motor (última ejecución de la bitácora + gasto del día contra el presupuesto), conteo exacto de piezas por estado y tendencias por puntuación. "Resultados de ayer" (tráfico) queda fuera hasta que exista una fuente real de tráfico (bucle de Search Console, Etapa 5) — cero datos inventados.
  - `Pluma\Admin\PantallaSalud` pasa a ser `PantallaPanel`: la página única del panel que arranca el shell React con URL/nonce de REST y todos los textos i18n; la pantalla de salud existente vive ahora dentro del shell como Sala de Máquinas.
  - Repositorios: `RepositorioTendencias::obtenerRecientes()`, `RepositorioBitacora::obtenerUltima()`, `RepositorioPiezas::contarPorEstado()` — solo lectura, sin cambios de esquema.
  - **La Sala de Tendencias** (Cap. 10.2, "el radar en vivo"): tarjetas con la Puntuación de Oportunidad desglosada en sus componentes reales (velocidad y afinidad — la tarjeta declara en pantalla que hueco competitivo y vida útil llegan con el Radar completo, deuda PLUMA-E1-1), quién la está cubriendo ya (artículos de las señales), y las tres acciones directas sobre la agenda: *Cubrir ahora* (prioriza la Pieza viva o crea una nueva prioritaria — el Orquestador ordena cada lote por prioridad antes que por antigüedad), *Ignorar* (descarta la Pieza vía Transicionador con auditoría y saca la tarjeta de la Sala) y *Vigilar* (descarta la Pieza sin gastar investigación/redacción/APIs pero mantiene la tendencia destacada en el radar). Una Pieza ya PUBLICADA nunca se toca.
  - `Pluma\Admin\RestSalaTendencias` (`GET /pluma/v1/tendencias`, `POST /tendencias/{id}/{cubrir,ignorar,vigilar}`): protegido con `pluma_aprobar_piezas` — intervenir la agenda es decisión editorial, no configuración del motor.
  - Esquema `0.8.0`: columna `estado` en `pluma_tendencias` (Cap. 11: en_pipeline/ignorada/vigilada) y `prioridad` en `pluma_piezas` con índice `estado_prioridad` — migración vía `dbDelta`, verificada contra instalación previa real.
  - **La Mesa Editorial** (Cap. 10.2, "el kanban de Piezas por estado"): las 13 columnas del grafo con la tarjeta de cada Pieza (tendencia, periodista, tesis corta, tono). Al abrir una Pieza: expediente completo (fuentes con extracto, url y nivel de verificación), Ficha de Decisión Editorial, desglose de Compuertas (calidad/riesgo/originalidad con motivos), e historial de borradores con diff línea a línea entre ciclos (`diff`/jsdiff, primera dependencia de producción real del panel — ver `LICENSES-THIRD-PARTY.md`). Cuatro acciones, todas auditadas: reasignar periodista (bloqueado sobre Piezas publicadas/descartadas), editar el borrador a mano (crea un ciclo nuevo marcado `editadoManualmente`, nunca reescribe el historial), descartar, y **forzar aprobación limitada a RETENIDA** (decisión del propietario, 2026-07-23: es literalmente el botón "Aprobar" de la Sala de Revisión — el grafo del Transicionador rechaza con 409 cualquier otro origen, ninguna ruta de código publica saltándose Compuertas).
- **Etapa 5 — La máquina que aprende**.
  - **Bucle de Search Console**: conexión OAuth2 real por instalación, selección del sitio verificado y sincronización de métricas reales (clics/impresiones/CTR/posición) hacia `pluma_metricas_search_console`, resolviendo cada URL a su Pieza real. Ver detalle completo en `docs/etapa-5-la-maquina-que-aprende.md`.
  - **Piezas de refuerzo y "dos golpes"** (Libro Cap. 3.4): el Radar ahora compara cada tendencia nueva contra las historias recientes con Pieza viva usando el proveedor de lenguaje económico (`Pluma\Sensores\ComparadorHistorias`), antes de crear una Pieza. Si la historia es idéntica a una ya cubierta, no se duplica. Si es la **evolución** de una historia ya cubierta (nuevo dato, giro, desmentido), la tendencia queda marcada "posible actualización" en la Sala de Tendencias — el sistema nunca crea la Pieza de actualización solo: el editor debe confirmarla con el nuevo botón **"Cubrir como actualización"**, que la enlaza a la Pieza original (`pieza_original_id`). La Mesa Editorial muestra "Actualización de la pieza #X" cuando corresponde. Paga la deuda `PLUMA-E1-2` (huella semántica de tendencias, abierta desde la Etapa 1).
  - **Memoria de audiencia + respuestas asistidas a comentarios** (Libro Cap. 5.7, "el sello del editor y el motor de conversación"): en cada tick, el Orquestador lee los comentarios reales de WordPress de las Piezas publicadas recientes, descarta el ruido (`Pluma\Redaccion\VerificadorComentarioSustantivo`) y, por cada comentario sustantivo nuevo, extrae con el proveedor de lenguaje económico un aprendizaje de audiencia (`Pluma\Redaccion\AnalizadorAudiencia`) que queda registrado en la memoria editorial del periodista (`TipoMemoria::Audiencia`, huérfana desde la Etapa 2, ahora poblada). Si el periodista tiene las **respuestas habilitadas** (nuevo interruptor por periodista en el Estudio de Conducta, deshabilitado por defecto), el sistema redacta además un borrador de respuesta en su voz (`Pluma\Redaccion\GeneradorRespuestaComentario`) que queda pendiente en la nueva **Sala de Comentarios** — el sistema nunca publica una respuesta por su cuenta: el editor la aprueba con un clic (se publica como comentario real, autor invitado con el nombre del periodista) o la descarta. Paga la deuda `PLUMA-E2-3`.
  - `Pluma\Publicacion\LectorComentarios`/`PublicadorComentario`: únicos puntos del plugin con `get_comments()`/`wp_insert_comment()`. `Pluma\Admin\RestRespuestasComentarios` (`GET /comentarios/pendientes`, `POST /comentarios/{id}/{aprobar,descartar}`, capacidad `pluma_aprobar_piezas`).
  - Esquema `0.12.0`: nueva tabla `pluma_respuestas_comentarios` (un comentario real se procesa como mucho una vez) y columna `respuestas_habilitadas` en `pluma_periodistas_conducta_versiones`.
  - **Informes editoriales semanales** (última pieza de la etapa): nueva pantalla "Informes Editoriales" que calcula bajo demanda una fotografía real de los últimos 7 días — piezas publicadas (total, por periodista, por vertical), retenidas/fallidas de la semana, tendencias por estado, salud del motor (ejecuciones, lotes, errores) y audiencia (comentarios procesados, aprendizajes, sentimiento, respuestas aprobadas/descartadas). Sin correo automático (decisión del propietario: el Libro no detalla el mecanismo de entrega) y sin sección de Search Console ni gasto semanal (ninguno de los dos tiene fecha real por fila o histórico persistido — cero invención, quedan como consumidores futuros bajo `PLUMA-E5-1`). `Pluma\Admin\RestInformesEditoriales` (`GET /panel/informes`, capacidad `pluma_configurar_motor`).
  - **Cerrada el 2026-07-24**: CI en verde en GitHub Actions (run [30068924188](https://github.com/jhonnfrank1995/PLUMA/actions/runs/30068924188)) — los 4 jobs (calidad PHP, calidad JS, integración wp-env, empaquetado con smoke real + E2E) en `success`. Criterio de salida del PLAN-MAESTRO cumplido: el sistema propone decisiones basadas en datos reales del sitio (posibles actualizaciones de "dos golpes", borradores de respuesta a comentarios) y el Informe Editorial las resume junto al resto de la actividad real de la semana. El primer intento de CI de esta etapa (run 30068583026) falló por una aserción de esquema hardcodeada en `.github/workflows/ci.yml` desde la Etapa 4 (`pluma_db_version` fijo en `'0.9.0'`, nunca actualizada al avanzar el esquema) — corregida leyendo la versión real desde `pluma-engine.php` en vez de un literal.
  - `Pluma\Admin\RestMesaEditorial` (`GET /piezas/kanban`, `GET /piezas/{id}`, `POST /piezas/{id}/{reasignar,editar,aprobar,descartar}`, capacidad `pluma_aprobar_piezas`). `Pluma\Pipeline\GestorSalaRevision::aprobar()`/`descartar()` generalizados con un `$origen` para que la auditoría distinga si la acción vino de la Sala de Revisión o de la Mesa Editorial.
  - Esquema `0.9.0`: columna `editado_manualmente` en `pluma_borradores` — distingue un ciclo del Corrector Interno de una edición humana en el historial de revisión (Cap. 11).
  - **El Banco de Periodistas + Estudio de Conducta** (Cap. 10.2, "la pantalla estrella"): tarjetas con métricas vivas reales (piezas publicadas y verticales donde más publica, extraídos de la Ficha de Decisión Editorial de sus Piezas publicadas — "tráfico medio" queda fuera hasta que exista Search Console, Etapa 5, mismo criterio que la Portada). Crear desde plantilla (los tres periodistas de siembra del Cap. 5.8), clonar, jubilar. Al abrir un periodista: el Estudio de Conducta completo — diales de temperamento como controles deslizantes, reglas cualitativas (línea editorial, líneas rojas, muletillas, vocabulario prohibido, trato al lector), matriz de tonos editable con la fila de Tragedia siempre bloqueada y de solo lectura (regla de sistema inviolable, Cap. 5.3), memoria editorial reciente navegable, y la **vista previa en vivo**: un párrafo de muestra que se re-redacta con la conducta candidata (todavía sin guardar) al mover cualquier control, con debounce de 800ms y sin repetir la llamada para la misma combinación exacta de diales — consume del mismo presupuesto diario compartido con la producción real, sin bypass ni pool separado (decisión del propietario, 2026-07-23).
  - `Pluma\Redaccion\GeneradorVistaPrevia` + `PropositoLenguaje::VistaPrevia` (modelo económico, no premium): redacta siempre sobre un hecho neutro fijo, nunca sobre una Pieza ni un expediente real — es una demostración de conducta, no periodismo de verdad. `Pluma\Admin\RestPeriodistas` (`GET /periodistas`, `GET /periodistas/{id}`, `GET /periodistas/plantillas`, `POST /periodistas/plantilla`, `POST /periodistas/{id}/{clonar,conducta,jubilar}`, `POST /periodistas/vista-previa`), capacidad `pluma_gestionar_periodistas` (la misma que ya protegía export/import). `Pluma\Datos\RepositorioPiezas::metricasPorPeriodista()`, solo lectura, sin cambios de esquema.
  - **La Sala de Revisión** (Cap. 10.2, "diseñada para decidir rápido: lectura limpia, diagnóstico arriba, tres botones"): bandeja de piezas RETENIDAS con su diagnóstico completo de Compuertas (motivos, calidad/riesgo/originalidad) y el borrador legible sin salir de la pantalla, más la cola de veto del modo Copiloto con cuenta regresiva en vivo hasta la ventana límite. Tres acciones sobre RETENIDAS (aprobar / devolver con nota / descartar) y un botón "Vetar" sobre la cola de veto que reutiliza el mismo `descartar` — expira también la ranura de `pluma_cola_publicacion` si hacía falta.
  - `Pluma\Admin\RestSalaRevision` (ya existente desde la Etapa 3) enriquecido: las respuestas de `/revision/retenidas` y `/revision/veto` ahora incluyen término de la tendencia, periodista asignado, el diagnóstico completo de Compuertas y el contenido del último borrador — sin cambios de esquema, solo lectura adicional vía los repositorios ya existentes.
  - Deuda nueva registrada en `docs/deuda.md` (PLUMA-E3-9): notificaciones por Telegram/Slack con enlaces de acción directa (Cap. 10.2) siguen sin construir — decisión ya tomada en la Etapa 3 de quedarse solo con correo, nunca antes registrada formalmente como deuda.
  - **La Sala de Máquinas completa** (Cap. 10.2): además de la salud del entorno heredada de la Etapa 0, ahora tiene bitácora del motor (últimas ejecuciones, duración calculada, lotes procesados, errores), coste de hoy contra el límite diario editable, estado de cada API conectada (OpenRouter: configurada o no + circuito; Google Trends: circuito), y la pantalla para cargar/probar/cambiar/quitar la llave de OpenRouter — **paga `PLUMA-E2-4`**, abierta desde la Etapa 2: cualquier instalación nueva ya puede activar redacción sintética real desde el panel, sin tocar `wp option update` a mano. "Coste por pieza" y "reintentos" quedan fuera — sin fuente real todavía (`PLUMA-E3-7`).
  - **Prueba en vivo de la llave** verificada contra la documentación oficial de OpenRouter: `GET https://openrouter.ai/api/v1/key` (créditos/límite restantes), sin invocar `LenguajeInterface::completar()` — no genera coste de redacción solo por validar una llave.
  - `Pluma\Proveedores\ProveedorOpenRouter::probarLlave()`/`circuitoAbierto()` y `Pluma\Proveedores\ProveedorGoogleTrends::circuitoAbierto()` (nuevos, solo lectura del circuit breaker ya existente). `Pluma\Datos\RepositorioBitacora::obtenerRecientes()` — de paso, corrige `obtenerUltima()`/`obtenerRecientes()` para devolver fechas en `DATE_ATOM` en vez de la cadena cruda de MySQL (bug latente desde la porción 1, sin efecto visible hasta ahora porque nada las parseaba como fecha).
  - `Pluma\Admin\RestSalaMaquinas` (`GET /motor/bitacora`, `GET /motor/estado`, `POST`/`DELETE /motor/llave-openrouter`, `POST /motor/llave-openrouter/probar`, `POST /motor/presupuesto`), capacidad `pluma_configurar_motor`. La llave nunca se devuelve en texto plano — solo un booleano "configurada" y, como mucho, sus últimos 4 caracteres.
  - `panel/src/PantallaSalud.tsx` renombrada a `PantallaSalaMaquinas.tsx` (mismo patrón que el rename de `PantallaSalud.php`→`PantallaPanel.php` en la porción 1).
  - **El Estudio SEO y Taxonomía** (Cap. 10.2): auditoría de canibalización real (todos los grupos de Piezas ya PUBLICADAS que comparten la misma keyword principal, con enlace directo a cada post) y salud taxonómica (categorías/etiquetas en cuarentena, y propuestas de fusión entre etiquetas casi-duplicadas que la reconciliación automática no fusionó por no compartir slug ni alias exacto). "Estado de indexación por pieza" y "keywords en el umbral 5-15" quedan fuera — sin fuente real todavía (Search Console, `PLUMA-E3-5`), mismo criterio que la Portada y el Banco de Periodistas. La pantalla es de solo lectura: muestra las propuestas de fusión pero no las ejecuta (fusionar de verdad implica reasignar términos en posts ya publicados).
  - `Pluma\Datos\RepositorioPiezas::obtenerCanibalizacion()` (nuevo, agregado vía `GROUP BY`, solo lectura, sin cambios de esquema). `Pluma\Taxonomia\ReconciliadorVocabulario::similitud()` pasa de privado a público (mismo umbral del 85%, `UMBRAL_SIMILITUD_PORCENTAJE` ahora también público) para que el panel reutilice exactamente la misma comparación que ya usa la reconciliación automática, sin duplicar lógica.
  - `Pluma\Admin\RestEstudioSeo` (`GET /seo/canibalizacion`, `GET /seo/vocabulario`), capacidad `pluma_configurar_motor` (visibilidad técnica del motor, mismo criterio que la Sala de Máquinas).
  - **Corrección de producción real**: `register_activation_hook` (que corre las migraciones de esquema) solo se disparaba en una activación manual — una actualización normal de WordPress reemplaza los archivos del plugin sin activarlo de nuevo, así que el esquema de un cliente real se habría quedado congelado en la versión con la que activó por última vez mientras el código corría migraciones más nuevas (reproducido localmente probando manualmente: esquema atascado en `0.3.0` contra código en `0.9.0`, con errores reales de SQL por columnas/tablas faltantes). `Pluma\Kernel\Activador::actualizarEsquemaSiHaceFalta()`, invocado ahora en cada `plugins_loaded`, cierra el hueco: compara la versión instalada contra la del código y solo vuelve a migrar (idempotente vía `dbDelta`) cuando hace falta.
  - **El asistente de instalación de 5 actos** (Cap. 10.3, última pantalla de la Etapa 4): verificación técnica real (versiones PHP/WordPress/base de datos, estado de WP-Cron, URL/token/comando exactos del cron real, recetas genéricas de cPanel y cron de sistema — sin detectar el proveedor de hosting, decisión explícita del propietario); conexión de la llave de OpenRouter con prueba en vivo (mismo bloque que la Sala de Máquinas, extraído a un componente compartido); línea editorial + importación real de las categorías existentes del sitio hacia el vocabulario de PLUMA; creación del primer periodista desde plantilla con ajuste fino de su Estudio de Conducta; elección de modo (Piloto pre-seleccionado de verdad, no solo visualmente) y ejecución del primer ciclo en vivo del motor delante del usuario. Afordancia "Saltar por ahora" en cualquier momento para quien ya configuró todo a mano.
  - `Pluma\Admin\RestOnboarding` (`GET /onboarding/estado-tecnico`, `POST /onboarding/{importar-categorias,modo,primer-ciclo,completar}`), capacidad `pluma_configurar_motor`. Único punto que ESCRIBE el modo de operación (hasta ahora solo se leía) y único importador real de categorías de WordPress hacia `pluma_vocabulario`.
  - `panel/src/BloqueLlaveOpenRouter.tsx`: extraído de `PantallaSalaMaquinas.tsx` para reutilizar el formulario de guardar/probar/quitar llave sin duplicarlo entre la Sala de Máquinas y el asistente.
  - `Aplicacion.tsx` gatea el shell completo detrás de `onboardingCompletado` (opción `pluma_onboarding_completado`, `false` en una instalación nueva): mientras no esté completo, el asistente reemplaza toda la interfaz — ninguna otra pantalla es alcanzable hasta terminar o saltar.
  - Sin cambios de esquema (0.9.0 sin cambios) — todo son opciones y filas de `pluma_vocabulario` ya soportadas por el esquema existente.
  - **Cerrada el 2026-07-23**: CI en verde en GitHub Actions (run [30043076644](https://github.com/jhonnfrank1995/PLUMA/actions/runs/30043076644)) — los 4 jobs (calidad PHP, calidad JS, integración wp-env, empaquetado con E2E) en `success`. Criterio de salida del PLAN-MAESTRO cumplido: el panel completo del Cap. 10 (7 pantallas) más el asistente de instalación de 5 actos del Cap. 10.3 quedan operativos contra WordPress real, con el motor ejecutando su primer ciclo en vivo delante del usuario al cerrar el asistente.
- **Etapa 5 — La máquina que aprende** (en curso): bucle de Search Console, memoria de audiencia, piezas de refuerzo y "dos golpes", respuestas asistidas a comentarios, informes editoriales semanales — entregada por porciones, empezando por la pieza más grande y de la que dependen las demás.
  - **Porción 1 — Bucle de Search Console** (paga parcialmente `PLUMA-E3-5`): conexión OAuth2 real con la cuenta de Google Search Console del propietario (cada instalación usa su propio client_id/client_secret de Google Cloud, como la llave de OpenRouter — nunca un secreto compartido embebido en el plugin), selección del sitio verificado, y sincronización real de métricas (clics, impresiones, CTR, posición por página+consulta de los últimos 28 días) verificada contra la documentación oficial de Google (OAuth2 web-server flow y Search Console API v3). Deliberadamente fuera de esta porción: los consumidores del dato (regenerar títulos débiles, candidatos a pieza de refuerzo, ajuste de asignación por periodista, hueco competitivo de `PuntuacionOportunidad`) — quedan registrados como deuda nueva (`PLUMA-E5-1`) para porciones futuras de esta misma Etapa, una vez exista suficiente histórico real.
  - `Pluma\Proveedores\ProveedorSearchConsole` (+ `TokensOAuth`, `FilaAnaliticaSearchConsole`): mismo patrón de resiliencia que `ProveedorGoogleTrends`/`ProveedorOpenRouter` (circuit breaker por fallos consecutivos, timeout explícito, sin `sleep()` síncrono).
  - Esquema `0.10.0`: tabla `pluma_metricas_search_console` (clics/impresiones/CTR/posición por post+consulta, con `pieza_id` resuelto vía `url_to_postid()` y nuevo `RepositorioPiezasInterface::obtenerPorPostId()` — nulo cuando la URL no pertenece a ninguna Pieza gestionada por PLUMA).
  - `Pluma\Admin\RestSearchConsole` (`GET /search-console/estado`, `POST`/`DELETE /search-console/credenciales`, `GET /search-console/callback`, `GET /search-console/sitios`, `POST /search-console/sitio`, `POST /search-console/sincronizar`), capacidad `pluma_configurar_motor`. El callback OAuth2 verifica un `state` de un solo uso (anti-CSRF) además de la capacidad real de WordPress — nunca `__return_true`. Las credenciales (client_id, client_secret, refresh_token) se cifran igual que la llave de OpenRouter; el access_token de corta vida se cachea en un transient, nunca se persiste.
  - `panel/src/BloqueSearchConsole.tsx`: formulario de credenciales con la URI de redirección exacta a registrar en Google Cloud, selector de sitio, sincronización manual y tabla de métricas recientes — integrado en la Sala de Máquinas junto al bloque de OpenRouter.
- **Etapa 6 — Producto en venta** (en curso): licenciamiento + updates firmadas, empaquetado reproducible, telemetría opt-in, docs de venta, beta cerrada — entregada por porciones. GOVERNANCE §5.4 (licenciamiento + servidor de actualizaciones propio) queda fuera de esta Etapa por decisión explícita del propietario, pospuesta al final del desarrollo (deuda `PLUMA-E6-1`, ver `docs/deuda.md`).
  - **Porción 1 — Versionado SemVer + empaquetado reproducible + matriz de compatibilidad** (GOVERNANCE §5.1-5.3): la versión del plugin vive en cuatro lugares que ahora se verifican automáticamente en cada commit (`tests/Unit/Datos/VersionConsistencyTest.php`) — el comentario `Version:` y la constante `PLUMA_ENGINE_VERSION` de `pluma-engine.php`, `composer.json["version"]`, `package.json["version"]`; ya hubo un desincronizado real entre los dos primeros al cerrar la Etapa 5.
  - **Mecanismo de reversa de esquema** (`Pluma\Datos\Esquema::sentenciasReversaDesde()` + `Pluma\Datos\Migrador::revertirA()`, nueva `ReversaNoDisponibleException`): toda transición de esquema declara explícitamente su reversa — nunca se infiere. La transición real `0.11.0→0.12.0` (Etapa 5) es el caso de referencia, verificado contra datos reales sembrados (`tests/Integration/MigracionConDatosRealesTest.php`); las transiciones anteriores no la tienen (deuda histórica no reconstruida retroactivamente). `docs/proceso-de-release.md` documenta el checklist obligatorio para todo bump de esquema futuro.
  - **`bin/build-zip`**: ahora construye el paquete de producción dos veces en directorios independientes y compara su huella (rutas + contenido) antes de publicar — si el mismo commit produce árboles distintos, falla en vez de publicar un ZIP no determinista. Produce además `pluma-engine-{version}.zip.sha256`. Corrige de paso un hueco real que la propia verificación destapó: el script nunca copiaba `composer.lock` al paquete, así que cada build re-resolvía dependencias "latest" en vez de las fijadas.
  - **Matriz de compatibilidad** (`.github/workflows/compatibilidad.yml`, nuevo, `bin/generar-wp-env-matriz`): 6 combinaciones reales — {WP mínimo, WP latest} × {PHP 8.2, PHP 8.3} sin plugin SEO (compatibilidad de plataforma) + {Yoast, Rank Math} en un baseline fijo (compatibilidad de convivencia SEO real, no mockeada, vía `tests/Integration/CompatibilidadSeoTest.php`). Vive en un workflow separado de `ci.yml`, disparado manualmente o en `release/**` — GOVERNANCE pide matriz testeada "por release", no en cada commit.
  - `docs/proceso-de-release.md` (nuevo): referencia operativa completa del proceso de release, incluido el checklist ESQUEMA de `AGENTS.md`.
  - **Porción 2 — Telemetría opt-in (consentimiento + payload) + Modo diagnóstico** (GOVERNANCE §5.5-5.6): nuevo interruptor "Telemetría" en la Sala de Máquinas, deshabilitado por defecto — el editor puede ver exactamente qué se compartiría (versiones, modo de operación, conteos agregados de periodistas/piezas; nunca contenido ni llaves) antes de activarlo. **El envío HTTP real queda diferido** — no existe todavía un servidor receptor propio (mismo motivo raíz que el licenciamiento, `PLUMA-E6-1`) — registrado como deuda nueva `PLUMA-E6-2`. Nuevo botón "Descargar reporte de diagnóstico": genera un JSON con versiones, bitácora reciente y conflictos detectados (hoy solo Yoast+Rank Math simultáneos, ampliable con evidencia real) para pegar en un ticket de soporte — nunca contenido de Piezas ni llaves.
  - `Pluma\Proveedores\TelemetriaInterface`/`ProveedorTelemetria` (construcción local del payload, sin envío), `Pluma\Kernel\DetectorConflictos`, `Pluma\Kernel\ExportadorDiagnostico` (nuevos). `Pluma\Admin\RestSalaMaquinas` gana `GET`/`POST /motor/telemetria` y `GET /motor/diagnostico`, misma capacidad `pluma_configurar_motor`.
  - Sin cambios de esquema — nueva opción de WordPress `pluma_telemetria_habilitada` (opt-in, `false` por defecto, registrada en `Activador::activar()` y purgada en `Desinstalador::purgar()`).
  - **Porción 3 — Documentación de venta** (GOVERNANCE §5.7): nueva carpeta `documentation/` en la raíz del repo con las cuatro piezas mínimas exigidas, como HTML autocontenido con diseño propio (no Markdown interno) — guía de instalación, guía del onboarding de 5 actos, referencia de las 8 pantallas del panel y FAQ de conflictos/límites conocidos, más un índice. Sistema visual compartido (`documentation/assets/estilo.css`) que reutiliza literalmente los tokens de color del panel real, con modo oscuro nativo.
  - **Plan de evolución + gobernanza**: `docs/PLAN-MAESTRO-EVOLUCION.md` incorpora los tres complementos al Libro (Nivel Dos/Tres/Cuatro) como Etapas 7-10 y un arco legal dentro de la 6. Cambios de gobernanza transversales: `GOVERNANCE §1.6` (Regla de Puntuaciones Compuestas), `§2.8` (independencia del verificador), `§1.5` (prohibición de cloaking de IA); `docs/puntuaciones.md` y `docs/decisiones/` (ADR) nuevos.
  - **Porción 4a — Cumplimiento del Art. 50 UE**: cada pieza publicada por PLUMA emite ahora en el `<head>` su documento JSON-LD (`NewsArticle`/`OpinionNewsArticle`/`AnalysisNewsArticle`) — que se construía desde la Etapa 3 pero nunca se emitía en una página real (**paga la deuda `PLUMA-E3-4`**) — y, sobre las piezas generadas y publicadas por el motor sin aprobación humana activa, un marcado de transparencia de IA legible por máquina (Reglamento UE de IA, Art. 50): la etiqueta `iptc.digitalSourceType` con el valor `trainedAlgorithmicMedia`. Piso de fábrica no desactivable; el cliente solo configura el formato del aviso visible (nueva sección "Transparencia y cumplimiento" en la Sala de Máquinas). Primer hook de frontend del plugin (`Pluma\Seo\EmisorEsquemaFrontend`), sin coste de consultas en render. La página de autor con identidad sintética y el refinamiento por tipo de aprobación quedan para las porciones 4b/4c.
  - **Porción 4b — Página de autor con identidad sintética**: primera página virtual del plugin (`Pluma\Seo\PaginaAutorPeriodista` — rewrite rule + `template_include`, integrada con `get_header()`/`get_footer()` del tema activo), una por periodista sintético activo, con la declaración fija e ineludible (`Pluma\Redaccion\DeclaracionIdentidadSintetica`) de que el nombre es una identidad editorial sintética generada por IA, no una persona física. El `author.url` del JSON-LD ahora enlaza a esta página cuando el nombre de la pieza resuelve a un periodista real (antes siempre ausente). **Paga la parte (b) de la deuda `PLUMA-E6-3`.** `CLAUDE.md` amplía explícitamente su lista del frontend público para incluirla.
  - **Porción 4c — "Aprobar ahora" en Copiloto**: nueva acción en la cola de veto de la Sala de Revisión — un editor puede aprobar activamente una pieza antes de que expire su ventana de veto, en vez de esperar. Una pieza publicada así ya NO lleva el marcado de transparencia de IA del frontend (misma excepción del Art. 50 que ya cubría al modo Piloto), y la auditoría distingue de forma auditable si cada publicación fue por aprobación humana activa o por expiración automática de la ventana. **Cierra por completo la deuda `PLUMA-E6-3` y el arco N.3.**
- **Etapa 7 — Endurecimiento del criterio** (en curso): retrofit crítico sobre `Compuertas`/`Redacción` ya en producción, siguiendo `docs/PLAN-MAESTRO-EVOLUCION.md`.
  - **Porción 1 — Puntuaciones: pisos no compensables (Nivel Tres K.1 + K.2)**: la Selección de Ángulo (`Pluma\Redaccion\SelectorAngulo`/`CandidatoTesis`) ya filtraba candidatos por un piso de sustento, pero `puntuacionTotal()` seguía diluyéndolo en una media simple de 4 factores — ahora es la media ponderada de solo los 3 factores de prioridad (originalidad 0.40, línea editorial 0.35, conversacional 0.25). La Compuerta de Calidad (`Pluma\Compuertas\CompuertaCalidad`/`DiagnosticoCalidad`) tenía el mismo defecto pero más grave: sustento y estructura completa aportaban puntos a una suma de 100 en vez de vetear — ahora son dos pisos binarios NO compensables; sin ambos, la puntuación es 0 sin excepción, y solo entonces se ponderan proporción interpretativa/legibilidad/voz. `docs/puntuaciones.md` actualizado.
  - **Porción 2 — Derecho de réplica previa y perfil de jurisdicción (Nivel Tres M.1 + N.1)**: la Compuerta de Riesgo detecta ahora si una pieza afirma algo negativo sobre una persona/organización identificable sin que ninguna fuente recolectada registre su postura — motivo de retención independiente, incluso con doble fuente ya verificada (`Pluma\Compuertas\CompuertaRiesgo`/`DiagnosticoRiesgo`). Nuevo perfil de jurisdicción configurable (`Pluma\Compuertas\RegimenResponsabilidad`, default de fábrica `Civil`): en régimen `Penal`, cualquier afirmación negativa sobre persona identificable exige retención humana y excluye el modo Autónomo, sin excepción. Nueva sección "Perfil de riesgo legal" en la Sala de Máquinas (`Pluma\Admin\RestRiesgoLegal` + `BloqueRiesgoLegal.tsx`). `GOVERNANCE §2.3` extendido para declarar el cambio.
  - **Porción 3 — Contrato de independencia de familia de modelo (Nivel Tres J.1-J.2)**: `Pluma\Proveedores\LenguajeInterface` gana `familiaDe()` — deriva la familia real de entrenamiento del slug de modelo (convención `{proveedor}/{modelo}` de OpenRouter), no una taxonomía inventada. Nuevo modelo verificador configurable, independiente del premium (`Pluma\Proveedores\EnrutadorModelos::modeloVerificador()`), y `Pluma\Proveedores\VerificadorIndependenciaEpistemica` — el test de arquitectura obligatorio de `GOVERNANCE §2.8`: falla explícito si Autónomo intenta activarse con redactor y verificador de la misma familia. Alcance deliberadamente limitado (ADR 0003): solo el contrato: no bloquea todavía ningún flujo real, la obligatoriedad dura espera validación empírica en Piloto. Nueva sección "Modelo verificador" en la Sala de Máquinas (`Pluma\Admin\RestModeloVerificador` + `BloqueModeloVerificador.tsx`). **Cierra la Etapa 7 completa.**
- **Etapa 8 — El razonamiento completo** (en curso): retrofit de mecanismo TIER 1 sobre el pipeline ya enviado, siguiendo `docs/PLAN-MAESTRO-EVOLUCION.md`. Entregada por porciones verticales por módulo.
  - **Porción 1a — Cerebro editorial: plantilla versionada, anclas de 3 tramos y matriz de diales (Nivel Dos A.2-A.4)**: nueva infraestructura de embeddings compartida (`Pluma\Proveedores\EmbeddingsInterface` + `ProveedorOpenRouter::embed()` sobre `POST https://openrouter.ai/api/v1/embeddings`, mismo circuito/llave/presupuesto que la completación de chat; `EnrutadorModelos::modeloEmbeddings()`, default `openai/text-embedding-3-small`; `Pluma\Proveedores\SimilitudVectorial::coseno()`) — base que reutilizarán A.5 y J.3 en la Porción 1b. `Pluma\Redaccion\CompiladorDirectrices::ancla()` deja de tener solo 2 extremos (bajo/alto, frase corta) y pasa a **3 tramos** (`[0,33)/[33,67)/[67,100]`) por cada dial continuo, cada uno con una directriz y un párrafo ancla real de calibración de tono, congelados en `src/Redaccion/references/anclas-diales.php`. El prompt se ensambla ahora como una `Pluma\Redaccion\PlantillaPrompt` versionada, separando lo que ningún dial toca (identidad, línea editorial, regla de oro contra la alucinación, vocabulario prohibido, bloqueo de sátira por sistema) de la traducción dial→directriz. Nueva `Pluma\Redaccion\MatrizCombinacionDiales`: las 4 combinaciones documentadas de dos diales altos a la vez (humor+agudeza crítica, vehemencia+empatía, sátira+densidad de datos, formalidad baja+vehemencia) añaden su propia directriz — no 28 pares a priori.
  - Sin cambios de esquema, sin endpoints REST ni cambios de panel en esta porción.
  - **Porción 1b — Prioridad de corrección, verificación determinista de trazabilidad y corpus de regresión de voz (Nivel Dos A.5-A.6 + Nivel Tres J.3)**: `Pluma\Redaccion\PuntoCorrector::ordenDeReparacion()` (A.6) fija el orden de REPARACIÓN (Hechos → MatrizYLineasRojas → SolapamientoNGrama → ProporcionInterpretativa → Voz → TitularHonesto), distinto del orden de verificación en que `CorrectorInterno::revisar()` los evalúa; `RedactorSintetico::redactarPasada()` reordena los puntos reprobados según esta prioridad antes de instruir la siguiente pasada. Nueva capa determinista de trazabilidad (J.3): `Pluma\Redaccion\SegmentadorUnidadesFactuales` (segmentación de oraciones sin proveedor de lenguaje, con guarda de abreviaturas y decimales) + `Pluma\Redaccion\VerificadorTrazabilidadDeterminista` (embeddings + `SimilitudVectorial::coseno()` contra cada extracto del expediente, opción `pluma_umbral_similitud_trazabilidad`) — `CorrectorInterno` gana esta capa como paso previo al punto "hechos": las frases sin respaldo aparente se señalan explícitamente en la petición al proveedor, nunca se descartan solas (falsos positivos de paráfrasis legítima). Cambio de constructor en `CorrectorInterno` (nueva dependencia) reparado en los 6 call sites existentes (`Nucleo.php` + 5 archivos de test). Corpus de regresión de voz (A.5): `Pluma\Redaccion\VerificadorRegresionVoz` (deriva semántica vía embeddings) + corpus mínimo de desarrollo en `tests/Fixtures/corpus-voz.php` (2-3 piezas por cada uno de los 3 periodistas sembrados de `PlantillasSiembra`, copy editorial original, ampliable con piezas reales en Piloto) + `docs/protocolo-corpus-voz.md` (protocolo documentado para la deriva semántica contra el proveedor real y la discriminación a ciegas — verificaciones que GOVERNANCE §4.4 prohíbe simular en tests automatizados). **Cierra la Porción 1 completa (A.2-A.6 + J.3) de la Etapa 8.**
  - **Porción 2 — Investigador máquina (Nivel Dos B.1-B.4 + Nivel Tres O.2 + L.1-L.2)**: `Pluma\Investigacion` gana el Algoritmo de Resolución de Disputas (B.1+B.2) — `Pluma\Investigacion\ResolutorDisputas` clasifica contradicciones entre hechos del expediente en `Cifra`/`Atribucion`/`Ocurrencia` (una sola llamada `PropositoLenguaje::Clasificar`); las de tipo `Ocurrencia` marcan ambos hechos `NivelVerificacion::Disputado` — primera vez que ese estado se puebla desde código real. Jerarquía de fuentes (B.3): `Pluma\Investigacion\NivelFuente` (A/B/C) + `ClasificadorNivelFuente` (listas configurables `pluma_fuentes_nivel_a`/`pluma_fuentes_nivel_b`) + `CalculadoraPesoEfectivo` (`nivel_fuente_base × decaimiento_temporal × factor_independencia`, con `factor_independencia` vía solapamiento n-grama entre extractos — cadena de citación). Detección de hueco con relevancia causal (B.4+O.2): `Pluma\Investigacion\DetectorHuecos` extrae la matriz de encuadres cubiertos (económica/humana/política/técnica/histórica/legal), filtra por sustento propio del expediente y por relevancia causal con los actores/hechos concretos de la tendencia — nueva propiedad `Expediente::$huecosDetectados`. Verificación de procedencia de la declaración (L.1): `Pluma\Investigacion\VerificadorProcedenciaDeclaracion` (heurística determinista: comillas/verbo de atribución + lista configurable `pluma_canales_oficiales`), nueva propiedad `HechoFuente::$procedenciaDeclaracion`, cableada en `InvestigadorMecanico`. L.2 (autenticidad audiovisual): solo el modelo de datos (`HechoFuente::$corroboracionAudiovisual`) — la heurística real queda bloqueada hasta que Sensores clasifique el tipo de medio de la tendencia (`PLUMA-E8-3`). **Tres piezas deliberadamente diferidas, decisión explícita del propietario** (ver `docs/deuda.md`): la búsqueda de tercera fuente de B.2 punto 2 (`PLUMA-E8-1`, requiere elegir un proveedor de búsqueda web), el `decaimiento_temporal` de B.3 (`PLUMA-E8-2`, bloqueado por `PLUMA-E1-1`), y la heurística real de L.2 (`PLUMA-E8-3`). Descubierto durante el Mission Lock: ningún lugar del código asigna nunca `NivelVerificacion::Verificado` — gap preexistente de Etapa 1 fuera del alcance literal de esta porción, registrado como `PLUMA-E8-4`.
  - **Porción 3 — Aritmética del Radar y asignación (Nivel Dos C.1-C.3)**: `Pluma\Sensores\PuntuacionOportunidad::calcular()` corrige el defecto de suma ponderada simple (C.1) — afinidad con la línea editorial ahora es una puerta binaria (`elegible = afinidad >= umbral_afinidad_minima`, opción `pluma_umbral_afinidad_minima`, default 15/100) antes de ponderar, no un sumando más; una tendencia totalmente ajena a la línea editorial ya no puede alcanzar puntuación alta solo porque velocidad/hueco/vida útil son altos. Reponderación honesta de los dos factores reales (velocidad 0.40, afinidad residual 0.15) — techo de la puntuación ahora es 55/100, no 100, mientras hueco competitivo (0.25) y vida útil (0.20) sigan sin construir (paga parcialmente `PLUMA-E1-1`). `Pluma\Redaccion\AsignadorPeriodista` (C.2+C.3): nuevo piso de dominio mínimo (`pluma_umbral_dominio_minimo_periodista`, default 40/100) — si ningún periodista lo supera, `NingunPeriodistaIdoneoException` en vez de asignar "al menos malo"; nueva cascada de desempate cuando el primero y el segundo candidato están dentro de un margen configurable (`pluma_margen_empate_asignacion`, default 5/100): balance de carga → historial con la historia específica (reutiliza `Pieza::$piezaOriginalId`, cero infraestructura nueva) → `AzarInterface` con semilla — nunca "el primero del array" (el bug de desempate más común, ya corregido). Nuevo estado lateral `EstadoPieza::SinPeriodistaIdoneo` (desde `EnRedaccion`, reanudable o descartable) con notificación por correo (`Pluma\Admin\NotificadorSinPeriodistaIdoneo`, mismo patrón que `NotificadorRevision`) y superficie en panel (etiqueta + alerta en Portada). **Decisión de propietario tomada al abrir esta porción**: no se integra Search Console como proxy de hueco competitivo (evita inventar un mapeo no pedido por el texto fuente); el decaimiento temporal de B.3 (Porción 2) y el consumo del gate `elegible` en el Orquestador quedan diferidos como deuda explícita (`PLUMA-E8-5`).
  - **Porción 4 — Prueba de Falseabilidad (Nivel Tres O.1)**: nueva Fase 3.5 del Algoritmo de Decisión Editorial, entre la selección de ángulo (Paso 3) y la arquitectura de la pieza (Paso 4). `Pluma\Redaccion\VerificadorFalseabilidad` (nuevo propósito `PropositoLenguaje::Falsear`, económico) construye, usando exclusivamente el expediente, el caso más fuerte posible EN CONTRA de la tesis ganadora — un intento genuino de derrotarla, no el contraargumento cortés que el Paso 4 ya contempla. `Pluma\Redaccion\DecisionEditorial::decidir()` orquesta el resultado: si la fuerza del caso en contra (evaluada por sustento verificable, no por elocuencia) supera el umbral de retorno (`pluma_umbral_regreso_falseabilidad`, default 75/100), la tesis se descarta y la decisión vuelve al Paso 3 a reevaluar entre los candidatos restantes (nunca se pierde toda la decisión); si es comparable a la tesis (misma fuerza o más, sin llegar al umbral) pero no la derrota, `FichaDecisionEditorial::$tensionFalseabilidad` registra explícitamente la tensión y `GeneradorEsqueleto` recibe el caso en contra para que el `contraargumentoReconocido` lo incorpore con el mismo peso argumental, no como concesión menor. Si todos los candidatos son derrotados, la Pieza no se fuerza a publicar "la menos derrotada" — se lanza excepción. Fila nueva en `docs/puntuaciones.md` §5, registrada antes de implementar (GOVERNANCE §1.6). Sin cambios de esquema.
  - **Porción 5 — Réplica dirigida (Nivel Tres M.2, Nivel 2-3): DIFERIDA a una versión posterior al lanzamiento comercial** (decisión de propietario, `ADR 0004`, 2026-07-27). Nivel 1 (verificación de postura ya existente en el expediente) ya está en producción desde la Etapa 7, sin cambios. Nivel 2 depende literalmente del mismo mecanismo de búsqueda web acotada que B.2 dejó diferido en la Porción 2 (`PLUMA-E8-1`); Nivel 3 tiene dependencias propias adicionales (canal de contacto verificado, cola de aprobación humana, ventana de espera integrada al grafo de estados) que tampoco existen hoy. Diseño completo documentado para retomar sin re-derivar contexto: `docs/funcionalidad-replica-dirigida-m2.md`. Deuda registrada: `PLUMA-E8-6`. Ningún código de producción cambia.
  - **Porción 6 — Grafo y memoria (Nivel Dos E.1 + E.2)**: E.1 (estado lateral `SinPeriodistaIdoneo`) ya se construyó junto con C.3 en la Porción 3 — sin cambios nuevos. E.2 (memoria colectiva del sitio): nuevo índice `tema` en solitario sobre `pluma_memoria_editorial` (esquema `0.14.0` — el índice compuesto existente `periodista_tema` tiene `periodista_id` como columna líder y no sirve para la consulta agregada por tema sin filtrar periodista). `Pluma\Datos\RepositorioMemoriaEditorialInterface::obtenerPosturasColectivasPorTema()` trae posturas de CUALQUIER periodista (activo o jubilado) sobre un tema. `Pluma\Redaccion\DecisionEditorial::decidir()` la consulta junto a la memoria individual, excluye la entrada del propio periodista asignado (ya cubierta por la memoria individual), y resuelve la atribución vía `RepositorioPeriodistasInterface::obtenerPorId()`: nuevo DTO `Pluma\Redaccion\PosturaColectiva` distingue si el autor sigue activo (atribución individual por nombre, "un colega... sostuvo") o está jubilado (atribución de sitio, "esta redacción sostuvo antes") — el sitio como voz colectiva ya no puede contradecirse a sí mismo solo porque quien firmó antes ya no está en el banco. `Pluma\Redaccion\SelectorAngulo::generarCandidatos()` gana un quinto parámetro (opcional, con default) que exige el mismo reconocimiento explícito de contradicción ya vigente para la memoria individual.
