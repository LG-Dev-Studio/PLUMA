# Registro de Deuda Técnica — PLUMA Engine
Regla (PLAN-MAESTRO): deuda sin etapa de pago asignada no existe — es omisión. Formato: | fecha | descripción | origen | etapa de pago | ticket |

| Fecha | Deuda | Origen | Pago | Ticket |
|---|---|---|---|---|
| 2026-07-22 | `tests/Integration/*` no cubre activación de red completa en multisitio real (solo cubierto vía dobles en `ActivadorTest` Unit); requiere `.wp-env.json` en modo multisitio, no configurado todavía | Etapa 0 — Cimientos | Etapa 3 (cuando Compuertas/Taxónomo añadan superficie real que justifique el entorno multisitio) | PLUMA-E0-1 |
| 2026-07-22 | `PuntuacionOportunidad` solo calcula Velocidad (35%) y Afinidad (30%), normalizados sobre el 65% disponible. Faltan Hueco competitivo (20%, necesita datos de SERP) y Vida útil (15%, necesita clasificación de perecibilidad) — decisión aprobada por el propietario al abrir la Etapa 1. Los datos reales de Search Console (Etapa 5, porción 1) y de memoria de audiencia (Etapa 5, porción 3) ya existen, pero ninguno de los dos se integró en la puntuación durante la Etapa 5 — ver `PLUMA-E5-1`. **Pagada parcialmente el 2026-07-27** en la Etapa 8 (C.1): la puerta de afinidad (`elegible = afinidad >= umbral_afinidad_minima`) y la reponderación honesta (0.40 velocidad / 0.15 afinidad residual, techo real 55/100) ya están en producción — decisión explícita del propietario de NO integrar Search Console como proxy de hueco competitivo en esta porción (evita inventar un mapeo que Nivel Dos C.1 no pide literalmente). Hueco competitivo (0.25) y vida útil (0.20) siguen sin construir | Etapa 1 — El esqueleto que camina | Etapa futura sin asignar todavía (la Etapa 5 cerró sin integrar ninguna de las dos fuentes reales en la puntuación; Etapa 8 C.1 pagó solo la puerta y la reponderación) | PLUMA-E1-1 |
| 2026-07-22 | ~~`RepositorioTendencias`/`SensorGoogleTrends` deduplican por término normalizado exacto, no por huella semántica (Libro Cap. 3.4: detectar la misma historia con distinto titular, o una historia que evoluciona)~~ — decisión aprobada por el propietario al abrir la Etapa 1. **Pagada el 2026-07-23** en la Etapa 5, porción 2: `Pluma\Sensores\ComparadorHistorias` compara cada tendencia nueva contra las historias recientes con Pieza viva vía el proveedor de lenguaje económico; idéntica → se descarta, evoluciona → queda marcada "posible actualización" a la espera de confirmación manual del editor (nunca automático), sin relación → sigue el flujo normal | Etapa 1 — El esqueleto que camina | Etapa 5 (La máquina que aprende) — **cerrada** | PLUMA-E1-2 |
| 2026-07-22 | `InvestigadorMecanico` no implementa el protocolo de 5 pasos del Cap. 4 (fuente primaria, 4-8 coberturas secundarias, triangulación, contexto, hueco) — el expediente de Etapa 1 usa directamente los artículos que el propio feed de Google Trends agrega, con nivel `Atribuido` único (fuente-agregador, no multi-medio independiente) | Etapa 1 — El esqueleto que camina | Etapa 3 (revisado al cerrar la Etapa 2: el criterio de salida de PLAN-MAESTRO para la Etapa 2 es el banco de periodistas + decisión editorial + redacción/Corrector + invariantes §2 + export/import, no el Investigador; la Etapa 3 ya trae Motor SEO/Taxónomo con superficie real para justificar un Investigador multifuente completo) | PLUMA-E1-3 |
| 2026-07-23 | `RedactorMecanico` (fallback de `RedactorConFallbackMecanico` cuando no hay presupuesto/credenciales del proveedor de lenguaje) sigue siendo el redactor mecánico rudimentario de la Etapa 1: sin periodista, sin diales, sin Corrector Interno — decisión explícita del propietario al abrir la Etapa 2 ("notificar y usar el redactor mecánico; en el futuro lo mejoramos a nivel máximo") | Etapa 2 — El periodista | Etapa futura sin asignar todavía (a decidir con el propietario cuándo priorizar mejorar el fallback en sí, no solo notificarlo) | PLUMA-E2-1 |
| 2026-07-23 | `AsignadorPeriodista::afinidadLineaEditorial()` usa un heurístico léxico simple (solapamiento de palabras ≥4 letras entre la línea editorial y tema+polaridad de la noticia), no comprensión semántica real — suficiente para el criterio de salida de la Etapa 2 (dos periodistas distinguibles a ciegas), pero un banco grande con líneas editoriales similares en superficie textual podría asignar mal | Etapa 2 — El periodista | Etapa 4/5 (cuando el presupuesto de coste y la telemetría de audiencia justifiquen invertir en una puntuación semántica real — embeddings o puntuación vía proveedor de lenguaje) | PLUMA-E2-2 |
| 2026-07-23 | ~~El "compromiso de respuesta" del Bloque del Editor (Libro Cap. 5.7: "el sistema puede, modo configurable, redactar borradores de respuesta del periodista a los primeros comentarios sustantivos") no está construido~~ — **pagada el 2026-07-23** en la Etapa 5, porción 3: `Pluma\Redaccion\GeneradorRespuestaComentario` redacta el borrador en la voz del periodista cuando tiene el interruptor "modo configurable" habilitado (`respuestasHabilitadas` por periodista, deshabilitado por defecto); el editor lo aprueba con un clic en la nueva Sala de Comentarios (`Pluma\Pipeline\GestorRespuestasComentarios`) antes de publicarse como comentario real — nunca automático | Etapa 2 — El periodista | Etapa 5 (La máquina que aprende) — **cerrada** | PLUMA-E2-3 |
| 2026-07-23 | ~~No existe todavía una pantalla de administración ni endpoint REST para que el cliente introduzca y cifre su llave de OpenRouter~~ (`ProveedorOpenRouter::OPCION_LLAVE_CIFRADA` solo se podía poblar vía `wp option update`/`Cifrado::cifrar()` manual) — sin esto, `RedactorSintetico` nunca tenía credenciales en una instalación real y el fallback mecánico se activaba siempre. **Pagada el 2026-07-23** en la Etapa 4, porción 6 (Sala de Máquinas): `Pluma\Admin\RestSalaMaquinas` ahora expone cargar/probar/cambiar/quitar la llave, cifrada con `Cifrado::cifrar()`, nunca en texto plano | Etapa 2 — El periodista | Etapa 4 (La experiencia premium: panel completo, cap. 10, es donde nace el resto de la configuración del motor vía UI) — **cerrada** | PLUMA-E2-4 |
| 2026-07-23 | Sitemap de noticias (protocolo Google News, últimas 48h) con ping de indexación no está construido — requiere registro de `rewrite rules` + flush en activación, infraestructura de enrutamiento propia distinta de `Pluma\Seo` puro que merece su propia verificación de activación/desactivación | Etapa 3 — La capa competitiva | Etapa 4 (cuando el panel/experiencia de instalación, cap. 10.3, ya maneja flush de rewrite rules en onboarding) | PLUMA-E3-1 |
| 2026-07-23 | Imagen destacada generada/seleccionada con banco de licencia y compresión WebP (Libro Cap. 6.2) no está construida — depende de un `Pluma\Proveedores\ProveedorImagenInterface` que todavía no existe (solo hay proveedores de lenguaje y de tendencias) | Etapa 3 — La capa competitiva | Etapa futura sin asignar todavía (a decidir con el propietario cuándo priorizar un proveedor de imágenes) | PLUMA-E3-2 |
| 2026-07-23 | Reformulación de H2/H3 como preguntas (candidatos a "People Also Ask", Libro Cap. 6.2) no está construida en `Pluma\Seo` — pertenece a tiempo de generación en `Pluma\Redaccion\GeneradorEsqueleto`, no a un post-proceso del Motor SEO (hacerlo ahí violaría "nunca reescribe argumentos") | Etapa 3 — La capa competitiva | Etapa futura sin asignar todavía (a decidir con el propietario si se aborda junto con una revisión de `GeneradorEsqueleto`) | PLUMA-E3-3 |
| 2026-07-23 | ~~`Pluma\Seo\ConstructorEsquemaNewsArticle` produce el documento JSON-LD (función pura, testeada), pero no hay todavía un hook de render en el frontend (`wp_head`) que lo invoque con los datos reales del post publicado~~ — **Pagada el 2026-07-25** en la Etapa 6, porción 4a (Art. 50 UE): `Pluma\Seo\EmisorEsquemaFrontend` (nuevo, primer hook de frontend del plugin) emite el JSON-LD en `wp_head` leyendo post meta persistidas por `Publicador` al publicar (cero consultas en render). Verificado en el `<head>` real del sitio dev, no solo en test | Etapa 3 — La capa competitiva | Etapa 6 (porción 4a) — **pagada** | PLUMA-E3-4 |
| 2026-07-23 | ~~Bucle de retroalimentación con Search Console (Libro Cap. 6.4: impresiones sin clics, keywords en posiciones 5–15, rendimiento por periodista) no está construido~~ — **pagada parcialmente el 2026-07-23** en la Etapa 5, porción 1: conexión OAuth2 real + sincronización de métricas (clics/impresiones/CTR/posición por página+consulta) hacia `pluma_metricas_search_console`, verificada contra la API oficial de Search Console. Los CONSUMIDORES del dato (regenerar títulos con impresiones altas y CTR bajo, candidatos a pieza de refuerzo desde keywords en posiciones 5-15, ajuste de asignación por rendimiento de periodista) todavía no existen — quedan como `PLUMA-E5-1` | Etapa 3 — La capa competitiva | Etapa 5 (La máquina que aprende) — **conexión cerrada, consumidores en `PLUMA-E5-1`** | PLUMA-E3-5 |
| 2026-07-23 | Consumidores reales del dato de Search Console (Etapa 5, porción 1 solo conecta y sincroniza): regenerar titulares SEO de piezas con impresiones altas y CTR bajo, candidatos a "pieza de refuerzo" desde keywords en posiciones 5-15, ajuste de asignación de periodistas por rendimiento real en su vertical, el componente de "hueco competitivo" (20%) de `PuntuacionOportunidad` (`PLUMA-E1-1`), y una sección de Search Console en el Informe Editorial semanal (Etapa 5, porción 4 — el bucle actual agrega por página+consulta, sin fecha real por fila, así que no se puede acotar a una ventana de 7 días sin inventar precisión) | Etapa 5 — La máquina que aprende (porción 1) | Etapa futura sin asignar todavía (la Etapa 5 cerró en la porción 4 sin construir ningún consumidor de este dato — cero histórico suficiente todavía para decidir umbrales sin inventarlos) | PLUMA-E5-1 |
| 2026-07-23 | "Modo pausa" (pausa global de un clic) y "modo respeto" (congela humor/sátira ante tragedia mayor, Libro Cap. 9.2) no están construidos en el Orquestador — ambos requieren un toggle en el panel de administración (aún no existe, Cap. 10) para que el editor los active manualmente; la degradación automática por sensibilidad (Cap. 8.2, `GestorDegradacion`) ya cubre el caso de bloqueo de sátira detectado por señal, no el modo respeto activado a mano | Etapa 3 — La capa competitiva | Etapa 4 (panel de administración) | PLUMA-E3-6 |
| 2026-07-23 | Reintentos con retroceso exponencial ante fallos de APIs externas (Libro Cap. 9.4) no están construidos — hoy un fallo de proveedor marca la Pieza `FALLIDA` en el intento actual (correcto y seguro) pero no reintenta automáticamente con backoff antes de darse por vencido; alertas tras 3 fallos tampoco existen todavía | Etapa 3 — La capa competitiva | Etapa futura sin asignar todavía (resiliencia de `Pluma\Proveedores`, no bloquea el criterio de salida de Etapa 3) | PLUMA-E3-7 |
| 2026-07-23 | Detección de WP-Cron real vs. cron del servidor + guía de instalación exacta para el hosting del cliente (Libro Cap. 9.4) no están construidas — el endpoint REST del motor (`RestOrquestador`) ya existe y funciona con cron real externo, pero no hay verificación activa de que WP-Cron esté deshabilitado ni instrucciones guiadas en el instalador | Etapa 3 — La capa competitiva | Etapa 4 (experiencia de instalación, Cap. 10.3) | PLUMA-E3-8 |
| 2026-07-24 | GOVERNANCE §5.4 (licenciamiento con verificación + servidor de actualizaciones propio con firmas) queda fuera del alcance de la Etapa 6 — requiere infraestructura de servidor fuera de este repo (emisión/validación de licencias, firma y hospedaje de ZIPs de actualización) que todavía no existe ni está diseñada. **Decisión explícita del propietario al abrir la Etapa 6 (2026-07-24)**: se documenta como deuda aceptada y se pospone al final del desarrollo; el resto de GOVERNANCE §5 (5.1 versionado, 5.2 empaquetado reproducible, 5.3 matriz de compatibilidad, 5.5 telemetría, 5.6 modo diagnóstico, 5.7 docs de venta) sí se construye en la Etapa 6. El criterio de salida de PLAN-MAESTRO ("GOVERNANCE §5 íntegro") se entiende parcial mientras esta deuda esté abierta — ver nota en `PLAN-MAESTRO.md` | Etapa 6 — Producto en venta | Etapa futura sin asignar todavía (fin del desarrollo, confirmado por el propietario) | PLUMA-E6-1 |
| 2026-07-24 | GOVERNANCE §5.5 (telemetría): el consentimiento opt-in y la construcción local del payload anónimo (`Pluma\Proveedores\ProveedorTelemetria::construirPayload()`) están completos, pero el **envío HTTP real queda diferido** — no existe todavía un servidor receptor, mismo motivo raíz que `PLUMA-E6-1` (licenciamiento). El editor puede ver exactamente qué se compartiría (`GET /motor/telemetria`) y activar el interruptor, pero hoy nada sale del sitio | Etapa 6 — Producto en venta (porción 2) | Etapa futura sin asignar todavía (junto con `PLUMA-E6-1`, cuando exista el servidor receptor) | PLUMA-E6-2 |
| 2026-07-25 | Rendimiento del modelo de datos ampliado no re-medido: las entidades nuevas del Nivel Cuatro (`Historia`, `EventoProgramado`, suscriptor, pista, comentario clasificado) multiplican el modelo de datos del Cap. 11 del Libro sin que nadie haya re-verificado los límites de rendimiento del Cap. 12 contra el nuevo total (el propio N4-III.3 predijo que este sería el hueco de un "Nivel Cinco") | Plan de Evolución (N4) | Etapa 9 — El medio real (al introducir las entidades nuevas) | PLUMA-EV-1 |
| 2026-07-25 | RGPD/LOPD de datos de lectores: el buzón de pistas (N4 X.3), las encuestas (X.2) y las suscripciones (W) introducen datos personales de lectores donde antes solo había contenido — consentimiento, retención, exportación, borrado. Requiere revisión legal humana, no solo ingeniería (N4-III.3 lo señala sin especificarlo) | Plan de Evolución (N4) | Etapa 9 — El medio real (junto a Comunidad/Confianza) | PLUMA-EV-2 |
| 2026-07-25 | Calibración empírica en Piloto de todos los umbrales de fábrica introducidos por los niveles: doble familia de modelo (N3 J, ADR 0003), fuerza del contraargumento (N3 O.1), disparador del modo respeto (N2 F.2), ventana del derecho de réplica nivel 3 (N3 M.3). Son hipótesis razonadas, no números probados — hito de validación explícito antes de convertirse en constantes de sistema (N2-I.2, N3-T.2) | Plan de Evolución (N2/N3) | Etapa 7-8 (contratos) con validación en el primer Piloto real | PLUMA-EV-3 |
| 2026-07-25 | ~~Art. 50 UE (Nivel Tres N.3), arco completo~~ — **pagada el 2026-07-25**. **(4a)** núcleo legal: marcado legible por máquina + emisión de frontend. **(4b)** página de autor por periodista con declaración de identidad sintética (`Pluma\Seo\PaginaAutorPeriodista`, primera página virtual del plugin, + `Pluma\Redaccion\DeclaracionIdentidadSintetica`); el JSON-LD enlaza `author.url` a la página real cuando el nombre resuelve a un periodista activo. **(4c)** columna `tipo_aprobacion` en `pluma_auditoria` (esquema `0.13.0`) + `aprobacion_activa` en `pluma_cola_publicacion` + acción "aprobar ahora" en la cola de veto de Copiloto (`GestorSalaRevision::aprobarAhora()`, endpoint `POST /revision/{id}/aprobar-ahora`): una pieza aprobada activamente por un humano antes de publicar ya NO lleva el marcado de IA del frontend (misma excepción que Piloto), y la auditoría distingue `humana_activa` de `automatica_por_expiracion`. Verificado de punta a punta contra WordPress real: pieza aprobada, tick real del motor, post publicado sin `iptc.digitalSourceType` | Etapa 6 — Producto en venta (porciones 4a/4b/4c) | Etapa 6 — **cerrada** | PLUMA-E6-3 |
| 2026-07-25 | Detectores de investigación activa que el sistema NO construye desde cero (heurística mínima + hito de profundización, no algoritmo completo): manipulación coordinada de tendencias (N2 G.1) y autenticidad audiovisual (N3 L.2). Ambos son problemas abiertos de la industria de confianza y seguridad; se implementa una heurística mínima defendible con compromiso declarado de profundizar, nunca un detector sofisticado fingido. **Actualización 2026-07-27 (Etapa 8, Porción 2)**: L.2 avanzó solo el modelo de datos — `Pluma\Investigacion\EstadoCorroboracionAudiovisual` en `HechoFuente`, default `NoAplica` — pero la heurística real sigue bloqueada: el Sensor/Radar no clasifica si una tendencia origen es audiovisual (ningún campo de tipo de medio existe hoy), así que `InvestigadorMecanico` no tiene ninguna señal con la que decidir cuándo aplicar el chequeo. Ver `PLUMA-E8-3` | Plan de Evolución (N2/N3) | Etapa 8 (heurística mínima; L.2 solo modelo de datos, heurística real espera `PLUMA-E8-3`); profundización en etapa futura sin asignar | PLUMA-EV-4 |
| 2026-07-23 | `Pluma\Admin\NotificadorRevision` solo notifica por correo (`wp_mail`) cuando una pieza queda RETENIDA — el Libro Cap. 10.2 pide "notificaciones por correo/Telegram/Slack con enlaces de acción directa". Decisión del propietario al construir la Sala de Revisión en la Etapa 3: solo correo por ahora (documentado en el propio código, nunca formalmente registrado como deuda hasta ahora). Enlaces de "acción directa" además exigen decidir cómo autenticar una aprobación/descarte disparada desde un clic en Telegram/Slack sin exponer un bypass de `pluma_aprobar_piezas` | Etapa 3 — La capa competitiva | Etapa futura sin asignar todavía (requiere además una pantalla de configuración de webhooks, hoy inexistente) | PLUMA-E3-9 |
| 2026-07-27 | Nivel Dos B.2 punto 2: para contradicciones de tipo `ocurrencia`, el protocolo exige "buscar una tercera fuente independiente antes de cerrar el expediente" — PLUMA no tiene ningún proveedor de búsqueda web (solo Google Trends y OpenRouter). **Decisión explícita del propietario al abrir la Porción 2 de la Etapa 8 (2026-07-27)**: diferir esta pieza hasta elegir y verificar un proveedor real (mismo tratamiento que la elección de proveedor de imagen de la Porción 8). `Pluma\Investigacion\ResolutorDisputas` marca toda contradicción de ocurrencia como `Disputado` directamente, sin el intento de resolución por tercera fuente | Etapa 8 — El razonamiento completo (Porción 2) | Etapa futura sin asignar todavía (cuando se decida y verifique un proveedor de búsqueda real) | PLUMA-E8-1 |
| 2026-07-27 | Nivel Dos B.3: el factor `decaimiento_temporal` de `peso_efectivo = nivel_fuente_base × decaimiento_temporal × factor_independencia` depende de la clasificación de vida útil de la tendencia (relámpago/ola/marea, `PLUMA-E1-1`) que no existe en ningún lugar del código — el propio Sensor todavía no la calcula. **Decisión explícita del propietario (2026-07-27)**: `Pluma\Investigacion\CalculadoraPesoEfectivo` implementa `nivel_fuente_base` (vía `ClasificadorNivelFuente`, listas configurables `pluma_fuentes_nivel_a`/`pluma_fuentes_nivel_b`) y `factor_independencia` (heurística de solapamiento n-grama entre extractos) ahora; `decaimiento_temporal` queda fijo en `1.0` (sin decaimiento) hasta que `PLUMA-E1-1` se resuelva. La clase no está todavía registrada en `Nucleo.php` porque ningún flujo real la invoca — mismo tratamiento que `VerificadorIndependenciaEpistemica` en la Etapa 7 (Porción 3) | Etapa 8 — El razonamiento completo (Porción 2) | Etapa futura sin asignar todavía (junto con `PLUMA-E1-1`, cuando el Sensor clasifique vida útil) | PLUMA-E8-2 |
| 2026-07-27 | El Sensor/Radar no clasifica si una tendencia origen es audiovisual (video/imagen) o textual — ningún campo de tipo de medio existe en `articulosRelacionados` ni en la tabla de tendencias. Bloquea la heurística real de L.2 (`PLUMA-EV-4`): sin esta señal, `InvestigadorMecanico` no puede decidir cuándo aplicar el chequeo de corroboración audiovisual; hoy todo hecho nuevo se marca `EstadoCorroboracionAudiovisual::NoAplica` por defecto, honesto pero incompleto | Etapa 8 — El razonamiento completo (Porción 2) | Etapa futura sin asignar todavía (requiere que Sensores clasifique tipo de medio de la tendencia primero) | PLUMA-E8-3 |
| 2026-07-27 | Triangulación real "2+ fuentes independientes → `Verificado`" (Libro Cap. 4.3, la regla original, no una pieza nueva de Nivel Dos/Tres): ningún lugar del código asigna nunca `NivelVerificacion::Verificado` — `InvestigadorMecanico` marca todo hecho nuevo `Atribuido` uniformemente. Detectar que dos fuentes reportan el MISMO hecho con REDACCIÓN DISTINTA (paráfrasis, no solapamiento textual) requiere comparación semántica (embeddings, ya disponibles desde la Porción 1) — fuera del alcance literal de B.1-B.4/L.1-L.2, que asumen esta base ya construida y solo la refinan. Descubierto durante el Mission Lock de la Porción 2, no antes | Etapa 1 — El esqueleto que camina (gap preexistente, descubierto en Etapa 8 Porción 2) | Etapa futura sin asignar todavía (a decidir con el propietario: comparación semántica de hechos vía embeddings) | PLUMA-E8-4 |
| 2026-07-27 | Nivel Dos C.1: `PuntuacionOportunidad::calcular()` ya expone `elegible`/`total` con la puerta de afinidad y el techo honesto de 55/100, pero **nada aguas abajo lo consume todavía** — `Orquestador::detectarTendencias()` sigue creando una Pieza para toda tendencia detectada, sin importar si es elegible. Requiere una decisión de producto (ocultar tendencias no elegibles en la Sala de Tendencias vs. mostrarlas con una insignia "no elegible") antes de cablear el gate — no se expandió el alcance de esta porción a `Pipeline`/`Admin` sin que se pidiera explícitamente | Etapa 8 — El razonamiento completo (Porción 3, C.1) | Etapa futura sin asignar todavía (decisión de producto sobre visibilidad en Sala de Tendencias) | PLUMA-E8-5 |
| 2026-07-27 | Nivel Tres M.2, Niveles 2 y 3 completos (réplica dirigida): búsqueda dirigida de la postura del señalado cuando ninguna fuente recolectada la trae (Nivel 2, mismo mecanismo que `PLUMA-E8-1`), y contacto directo automatizado con ventana de espera (Nivel 3, requiere además canal de contacto verificado, cola de aprobación humana y ventana de espera temporizada en el grafo de estados — ninguna de las tres existe hoy). **Decisión explícita del propietario (`ADR 0004`, 2026-07-27): toda la Porción 5 de la Etapa 8 queda diferida a una versión posterior al lanzamiento comercial**, no solo bloqueada técnicamente. El Nivel 1 (verificación de postura ya existente en el expediente) ya está en producción desde la Etapa 7 (M.1+N.1) y no se ve afectado. Documentación completa de la funcionalidad diferida en `docs/funcionalidad-replica-dirigida-m2.md` | Etapa 8 — El razonamiento completo (Porción 5, M.2 Niveles 2-3) | Versión posterior al lanzamiento comercial (`ADR 0004`); Nivel 2 junto con `PLUMA-E8-1` cuando se elija proveedor de búsqueda | PLUMA-E8-6 |
